version: '3.9'

services:
  web:
    image: nginx:latest
    ports:
        - "8080:80"
    volumes:
        - .:/code
        - ./site.conf:/etc/nginx/conf.d/site.conf
    links:
        - php
php:
    image: php:8.0.24-fpm-alpine3.16
    volumes:
        - .:/code